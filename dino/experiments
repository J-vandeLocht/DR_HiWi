Also use kaggle fine-tuning on DINOv3 models
- classifier might be marginally better
- but MIL model is probably marginally worse
-> only use the fine-tuning on our data, not the additional kaggle fine-tuning

Use Temporal Segment Sampling during Training
- does not seem to help performance. Maybe because best results are often achieved after only a single epoch so different sampling does not change much
-> Also does not hurt performance; theoretical sound so probably use it

Standard DINOv3 augmentations (basically none) vs. our more complex
- same performance but less severe (or none) overfitting for the classifier
- also very similar performance
-> keep complex augmentations in the hope of better generalization on unseen data (from other smartphones and adapters perhaps)

